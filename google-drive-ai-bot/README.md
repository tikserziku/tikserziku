# 🤖 Google Drive AI Bot — Apps Script

Telegram-бот: поиск по Google Drive + Gemini 2.0 Flash + память в Sheets.

## Быстрый деплой

1. **[Открыть script.google.com](https://script.google.com)** → New project
2. Вставить `Code.js` (CONFIG уже заполнен)
3. **Run** → `testGemini` → дать OAuth разрешения
4. **Deploy** → New deployment → Web app (Execute as: Me, Access: Anyone)
5. **Run** → `setTelegramWebhook`
6. Написать боту `/start`

## Команды бота

| Команда | Действие |
|---------|----------|
| Любой текст | Drive поиск + ответ Gemini |
| `/search запрос` | Список файлов |
| `/memory` | История разговора |
| `/clear` | Очистить память |
| `/stats` | Статистика |

## Архитектура

```
Telegram → Apps Script (webhook)
              ├── DriveApp.searchFiles()  ← ищет по всем твоим Docs/Sheets
              ├── DocumentApp.openById()  ← читает содержимое
              ├── Gemini 2.0 Flash API    ← генерирует ответ
              └── Google Sheets           ← хранит память сессии
```

## Phase 2 (следующие шаги)

- [ ] Firestore вместо Sheets для масштабирования
- [ ] Embedding cache (SHA-256 хэш чанков → Firestore)
- [ ] NotebookLM — загружать суммари разговоров
- [ ] AppSheet мобильное приложение для клиентов

## Стоимость

**$0** на Google Free Tier:
- Apps Script: 90 мин CPU/день бесплатно
- Gemini 2.0 Flash: ~$0.0004 за запрос
- Google Drive/Sheets: бесплатно
